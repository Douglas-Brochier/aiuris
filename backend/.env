# Variáveis de Ambiente para o Backend

# Configuração do Banco de Dados PostgreSQL
DB_USER=postgres_user
DB_HOST=localhost
DB_DATABASE=saas_juridico_mvp
DB_PASSWORD=strong_password_123 # Trocar por uma senha segura
DB_PORT=5432

# Configuração da Aplicação
PORT=3000 # Porta onde o backend Node.js vai rodar

# Segredo para JWT (JSON Web Token)
JWT_SECRET=your_very_secret_jwt_key_change_this # Trocar por um segredo forte e único
JWT_EXPIRES_IN=1h # Tempo de expiração do token (ex: 1h, 7d)

